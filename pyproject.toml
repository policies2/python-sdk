[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "policies2"
version = "0.1.0"
description = "Execute stored policies and flows over REST using API keys"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
